/*

describe("CinemaStore", function() {
	
	var cinemaStore;
	
	beforeEach(function() {
		cinemaStore = new CinemaStore();
		
	});
	
	it ('should return all the booking info', function(){
		var bookingTestArray=[];
		bookingTestArray=cinemaStore.getAllBookings();
		
		expect(bookingTestArray).not.toEqual(null); 
	});
});
*/